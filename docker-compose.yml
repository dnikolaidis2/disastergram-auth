version: '3.7'

services:
  auth:
    build: .
    ports:
    - "5000:5000"
    depends_on:
      - db

  # https://hub.docker.com/_/postgres
  db:
    image: postgres:11
    restart: always
    ports:
      - "6000:5432"
    environment:
      POSTGRES_PASSWORD: 1234